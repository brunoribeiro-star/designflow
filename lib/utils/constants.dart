import 'package:flutter/material.dart';

// Cores do app
class AppColors {
  static const primary = Colors.deepPurple;
  static const accent = Colors.amber;
  static const background = Color(0xFFF6F6F6);
  static const statusGreen = Colors.green;
  static const statusYellow = Colors.orange;
  static const statusRed = Colors.red;
}

// Textos fixos
class AppTexts {
  static const appTitle = "Personal Organizer Designers";
  static const noProjects = "Nenhum projeto cadastrado ainda.";
  static const noClients = "Nenhum cliente cadastrado.";
  static const noServices = "Nenhum tipo cadastrado.";
}